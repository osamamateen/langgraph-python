[project]
name = "langgraph-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "langgraph>=1.2.6",
    "requests>=2.34.2",
]
